name: CLI Release

on:
  push:
    tags:
      - "cli-v*"
  workflow_dispatch:
    inputs:
      version_bump:
        description: Version bump type
        required: false
        default: patch
        type: choice
        options:
          - patch
          - minor
          - major

concurrency:
  group: cli-release
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event_name == 'workflow_dispatch' && 'main' || github.ref }}

      - uses: cachix/install-nix-action@v31

      - name: Build CLI
        run: nix develop .#ci --command just cli-build-release

      - name: Get version
        id: version
        run: |
          VERSION=$(nix develop .#ci --command node -p "require('./cli/package.json').version")
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Verify tag matches package version
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          TAG="${GITHUB_REF_NAME}"
          if [ "$TAG" != "cli-v${VERSION}" ]; then
            echo "Tag ${TAG} does not match package version ${VERSION}"
            exit 1
          fi

  release:
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'workflow_dispatch'
    permissions:
      contents: write
    outputs:
      version: ${{ steps.version.outputs.new_version }}
      ref: ${{ steps.commit.outputs.ref }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: main
          token: ${{ secrets.CI_PAT }}

      - uses: cachix/install-nix-action@v31

      - name: Bump CLI version
        id: version
        env:
          VERSION_BUMP: ${{ github.event.inputs.version_bump || 'patch' }}
        run: |
          git fetch origin main --tags
          NEW_VERSION="$(nix develop .#ci --command ./scripts/version/bump-cli.sh "$VERSION_BUMP")"
          echo "new_version=${NEW_VERSION}" >> "$GITHUB_OUTPUT"
          echo "New version: ${NEW_VERSION}"

      - name: Build man page
        run: nix develop .#ci --command just cli-build-man

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Commit and tag
        id: commit
        run: |
          VERSION="${{ steps.version.outputs.new_version }}"
          TAG="cli-v${VERSION}"

          git add cli/package.json cli/man/delta.1

          if git diff --cached --quiet; then
            echo "No version file changes to commit"
          else
            git commit -m "chore(cli): release v${VERSION} [skip ci]"
            git fetch origin main --tags
            git rebase origin/main
            git push origin HEAD:main
          fi

          if git ls-remote --tags origin "refs/tags/${TAG}" | grep -q "${TAG}$"; then
            echo "Tag already exists on origin: ${TAG}"
          else
            git tag -a "${TAG}" -m "CLI v${VERSION}"
            git push origin "${TAG}"
          fi

          echo "ref=${TAG}" >> "$GITHUB_OUTPUT"

  npm-publish:
    runs-on: ubuntu-latest
    needs: [build, release]
    if: github.event_name == 'workflow_dispatch'
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.release.outputs.ref != '' && needs.release.outputs.ref || github.ref }}

      - uses: actions/setup-node@v4
        with:
          node-version: "24"

      - uses: cachix/install-nix-action@v31

      - name: Build publish bundle
        run: nix develop .#ci --command just cli-build-publish

      - name: Check if version already published
        id: published
        run: |
          VERSION=$(node -p "require('./cli/package.json').version")
          if npm view "@barrettruth/delta@${VERSION}" version >/dev/null 2>&1; then
            echo "published=true" >> "$GITHUB_OUTPUT"
          else
            echo "published=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish to npm
        if: steps.published.outputs.published != 'true'
        working-directory: cli
        run: npm publish --provenance --access public

  npm-publish-tag:
    runs-on: ubuntu-latest
    needs: build
    if: startsWith(github.ref, 'refs/tags/cli-v')
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}

      - uses: actions/setup-node@v4
        with:
          node-version: "24"

      - uses: cachix/install-nix-action@v31

      - name: Build publish bundle
        run: nix develop .#ci --command just cli-build-publish

      - name: Check if version already published
        id: published
        run: |
          VERSION=$(node -p "require('./cli/package.json').version")
          if npm view "@barrettruth/delta@${VERSION}" version >/dev/null 2>&1; then
            echo "published=true" >> "$GITHUB_OUTPUT"
          else
            echo "published=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish to npm
        if: steps.published.outputs.published != 'true'
        working-directory: cli
        run: npm publish --provenance --access public
